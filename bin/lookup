#!/usr/bin/env ruby
lib_dir = File.expand_path('../../lib', __FILE__)
$:.unshift lib_dir unless $:.include? lib_dir
require 'oui'
ARGV.map do |mac|
  if r = OUI.find(mac)
    puts r[:organization]
  end
end
